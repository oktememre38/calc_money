name: calc_money
description: "Gelir-gider takip uygulaması (Flutter, Türkçe, cihaz içi SQLite)."
publish_to: "none"
version: 0.6.1+9

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ">=0.19.0 <0.21.0"
  path: ^1.9.0
  provider: ^6.1.2
  sqflite: ^2.3.3
  timezone: ^0.9.4
  flutter_local_notifications: ^17.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
